<div>
        <%= cl_image_tag(@list.photo.key, :transformation=>[
          {:width=>1500, height: 500, :crop=>"scale", :effect=>"art:zorro"},
          {:overlay=>{:color=>"white", :font_family=>"Roboto", :font_size=>60, :text=>@list.name}}
          ]) %>
        </a>
  </div>

<% @list.movies.each do |movie| %>
<%= movie.title %>
<% end %>
<br>
<% @list.bookmarks.each do |bookmark| %>
<%= bookmark.comment %>
<% end %>

<%= link_to "Add A Movie", "#" %>

<%= link_to "Delete bookmark",
            list_path,
            method: :delete,
            data: { confirm: "Are you sure?" } %>
